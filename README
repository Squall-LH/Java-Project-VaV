Projet Vol au Vent

user de base pour mysql : vol
mdp de base pour mysql : vent
remplir la base : ant fill_database
vider la base : ant erase_database

Note pour plus tard : 
Pour que la tâche deploy fonctionne -pour le moment il n'y a rien a déployer de toute façon hein- , il faut créer le fichier localconfig.xml à la racine du projet.
Exemple de localconfig.xml :

<?xml version="1.0" encoding="UTF-8"?>
<project>
	<property name="dst.dir" value="/home/myHome/public_html/${project.name}" />
</project>

N'ajoutez pas le localconfig.xml au répertoire git...


